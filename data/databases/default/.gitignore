# Local, machine-specific data assets (imported from the Transfermarkt pipeline).
# The game resolves these relative to its working directory; they are regenerated
# by the data pipeline and should not live in git.
faces/
database.sqlite
database.sqlite-wal
database.sqlite-shm
manifest.json
images_competitions/
images_teams/
images_countries/
eredivisie.png