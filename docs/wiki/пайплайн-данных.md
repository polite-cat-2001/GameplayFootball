# Пайплайн данных

## Что это

Сводный **договор конвейера** Transfermarkt → GameplayFootball: из каких звеньев-репозиториев он
состоит, кто что производит, какими форматами звенья стыкуются, в каком порядке запускается полный
прогон, и как версионируются данные. Это **единственный дом**, описывающий картину целиком: агент,
которому надо понять «откуда берутся данные», начинает здесь; предметные детали конкретного формата
и маппинга — на связанных страницах и в документации самих звеньев (команды инструментов здесь **не
дублируются**, чтобы не расходиться — за ними идут в README/AGENTS конкретного репозитория).

Связанные страницы: [[данные-из-transfermarkt]] (канон, маппинг в GF SQLite, киты, калибровка),
[[база-данных]] (схема БД и её потребление игрой), [[смежные-проекты]] (экосистема), [[релиз]]
(упаковка), [[открытые-вопросы]] (статус и программа data v2).

## Лента конвейера

```
transfermarkt.com
   ▼  (HTML + внутренние JSON-API, живые запросы, кэша в API нет)
transfermarkt-api            форк FastAPI-обёртки; ни файлов не пишет, кормит скрейпер
   ▼  HTTP + CDN-картинки
transfermarkt_scrapper       краул → resume-кэш → canon-срез
   ▼  data/canon/*.json (плоский, TM-id) + data/images/* (в т.ч. флаги стран/национальностей)
ratings-generator ─┐         (параллельны и независимы друг от друга)
kit-generator     ─┼──►  спеки + PNG/фото
tm-gf-face-generator ┘
   ▼
tm-gf-import                конвертер «канон → игровой каталог данных»
   ▼
GameplayFootball/data/databases/default   ← потребляет игра
```

## Звенья (кто владеет чем)

Все репозитории — рядом на машине, `Desktop/projects/<имя>`.

| Звено | Роль | Граница выхода (что пишет) | Потребители выхода | Док внутри репо |
|---|---|---|---|---|
| **transfermarkt-api** | живой скрейпинг TM (FastAPI). Форк upstream + сборные/тренеры | JSON-эндпоинты (файлов не пишет) | скрейпер | `README.md`, `docs/wiki/` |
| **transfermarkt_scrapper** (`scraper-v2`) | краул TM через api + CDN, resume-кэш, разведка лиг | **плоский canon** `data/canon/*.json` на TM-id (7 файлов: meta, competitions, clubs, national_teams, players, players_market_value, coaches; один рекорд на игрока, дедуп «основной+II» схлопнут), `data/images/` (лица, логотипы, эмблемы, чёткие флаги стран по TM-id и национальностей по имени), сервисные (`tm_cache.sqlite3`, индексы) | все четыре генератора/импорт | `AGENTS.md`, `docs/wiki/` |
| **ratings-generator** | OVR/22 стата/POT по TM-id из рыночной стоимости | один JSON по TM-id игрока (шкала 0–99) | tm-gf-import | `AGENTS.md` |
| **kit-generator** | палитра + 6 комплектов PNG по `template_kit.png`; ручной web-редактор | `out/**/specs.json` + PNG по клубу/сборной; `export_game.py` раскладывает `images_teams/<лига>/<клуб>_kit_{main,white,black,reserve,gk1,gk2}.png` | tm-gf-import | `README.md`, `AGENTS.md`, `NOTES.md` |
| **tm-gf-face-generator** | внешность игрока (`skincolor`/`hairstyle`/`haircolor`) из фото/национальности; ручной редактор | `out/specs.json` (по TM-id игрока) | tm-gf-import | `README.md`, `NOTES.md` |
| **tm-gf-import** | **конвертер в игровой каталог данных** (исторический `build_gf_database.py` из вики скрейпера не существует — роль исполняет он) | готовый `data/databases/default/`: `database.sqlite`, `images_teams/`, `images_competitions/`, `faces/`. Правила именования файлов — код: `tm-gf-import/builders/files.py` | GameplayFootball | git-репо; док — `README.md`, `AGENTS.md` (`PROMPT.md` помечен устаревшим) |
| **GameplayFootball** | конечный потребитель | — | — | вики, этот файл |

### Границы контракта

Контракт между звеньями — **файлы, которые производитель пишет, а потребитель читает**. Менять
формат выхода может только владелец звена, и только синхронно с миграцией всех потребителей.
Схема выходных JSON звеньев описана в их доке; именование картинок — в коде `tm-gf-import/builders/files.py`
(источник истины — код, не проза). Три ключевых инварианта на всех границах:

1. Ключ всех сущностей — **TM-id** (см. [[глоссарий]]). БД игры хранит его в колонках
   `tm_id` (players/teams/leagues/countries) — см. «Идентичность» ниже.
2. Игра потребляет **SQLite-схему** (`[[база-данных]]`) + медиакаталог; схема создаётся в коде
   `src/menu/mainmenu.cpp` (CREATE TABLE) и `src/league/leaguecode.cpp`, отдельных `.sql` нет.
3. Файлы в git не хранятся (см. «Версионирование»).

## Полный прогон (порядок)

1. Поднять `transfermarkt-api` локально.
2. `transfermarkt_scrapper`: разведка/разметка лиг → краул профилей и составов → сборка
   **плоского канона** (`build_canon_from_cache.py`: resume-кэш → `data/canon/*.json`,
   идемпотентно, валидация `canon_schema.json`) → докачка картинок → флаги
   (`flags.py`: `data/images/country_flags/<tm_id>.png` + `nationality_flags/<slug>.png`).
3. Генераторы (`ratings`/`kit`/`faces`) — **параллельно**: каждый читает `data/canon/` и картинки.
4. `tm-gf-import` — последний: собирает БД + раскладывает файлы в целевой каталог данных игры,
   включая страновые флаги в `images_countries/<tm_id>.png`.
5. Проверка: запуск игры против **staging-каталога** данных, затем подмена `databases/default`.
6. Бандл данных: `tools/release/package_data.py` → `GameplayFootball-data-<ver>.zip` + строка в
   `data-versions.json`.

Полные команды каждого шага — в доке соответствующего репозитория.

## Идентичность

- **TM-id** — единственный стабильный внешний ключ на всех границах конвейера (имя файла фото,
  ключ JSON, id лиги в имени `images_competitions/<league_tm_id>.png`).
- Схема БД v2 (с 2026-09-09) хранит **`tm_id`** в players/teams/leagues/countries (NULL только у
  синтетических строк: страна «International», лига «National Teams»). Внутренние rowid БД игры
  (`teams.id`, `players.id` …) — **не контракт**: автоинкремент, меняются при каждой пересборке.
  Игра читает только rowid; `tm_id` — метаданные для внешнего матчинга (сеть, сохранения, апдейты
  данных). Правило: **матчинг сущностей в конвейере — только по TM-id в данной категории; имя —
  для отображения и логов** (фантом «Roberto Owono», спеллинг «Soulisack/Soulisak» — эталоны
  того, почему имя ненадёжно).
- Требование стабильности продиктовано будущим: LAN-матчи (хост шлёт клиентам снимок команд с
  хоста) и карьера (составы меняются во время игры). Стабильный ключ игрока — предпосылка формата
  «снимка команды» для провода.

## Версионирование данных

Git сгенерированные данные **не хранит** (и не должен — сотни МБ). В git живут код, вики и
**реестр** версий; сами данные — версионированные релиз-бандлы. Два уровня версий:

| Уровень | Где | Когда меняется |
|---|---|---|
| `schema_version` | `PRAGMA user_version` в БД + `manifest.json` | меняется схема/смысл колонок → нужна совместимая версия игры |
| `data_version` + `snapshot_id` | `manifest.json` рядом с БД | каждый новый срез составов/форм |

**Сделано (2026-09-09)**: `tm-gf-import` ставит `PRAGMA user_version = 2` и пишет
`manifest.json` (`schema_version`, `data_version`, `snapshot_id` = дата среза + git-хаши
инструментов, coverage-счётчики, sha256 БД). Игра на старте жёстко сверяет `user_version`
с константой `databaseSchemaVersion` (несовместимая схема → понятная ошибка, а не молчаливая
поломка) и логирует `data_version` из манифеста (`src/gamecontext.cpp`).

Распространение — `GameplayFootball-data-<ver>.zip` через релизы (см. [[релиз]]).
Реестр версий (кто когда что выпустил, checksum, совместимая версия игры) —
`data-versions.json` в репозитории игры; строка добавляется `tools/release/package_data.py`.

**Применение бандла**: zip содержит **содержимое** `databases/default` (без префикса каталога:
`package_data.py` кладёт `relpath` от `data_dir`). Распаковывать нужно **внутрь**
`databases/default/` (`unzip -o ... -d databases/default`), а не в каталог сборки: игра читает
картинки по путям `databases/default/images_*` (`src/menu/startmatch/teamselect.cpp:43,69,97`),
и без этого логотипы лиг/клубов и флаги стран не показываются (fallback — `orange.jpg`), а
`manifest.json` не находится.

## Текущее состояние (data v2 — закрыто)

- **Прод-формат канона — плоский `data/canon/*.json` на TM-id** (один рекорд на игрока,
  дедуп дублей). Вложенный `data/full/*.json` — legacy, больше не контракт.
- Возраст в canon НЕ несётся (TM-age ненадёжен): потребители выводят его из `birth_date` на дату
  сбора (`meta.snapshot_date`). Схема БД v2 (`tm_id`), `PRAGMA user_version` + `manifest.json`,
  бандлы и реестр — сделаны (2026-09-09). Детерминизм x86 перебазирован под canon (linux/x64/macos
  — переснять на своих машинах, см. [[открытые-вопросы]]).

## Правила поддержки этой страницы

- Новое звено конвейера или новый формат на границе → обновить таблицу и связанную страницу
  звена тем же изменением.
- Смена потребителя формата → синхронная миграция звеньев, фиксируется здесь и в [[открытые-вопросы]].
- Запрещено: складывать сгенерированные данные в git, дублировать команды звеньев в прозе, называть
  конвертер `build_gf_database.py`.
